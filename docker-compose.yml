services:
  db:
    image: postgres:16
    container_name: html_tutor_db
    environment:
      POSTGRES_DB: html_tutor
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
